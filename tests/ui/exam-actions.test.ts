import { describe, it } from "vitest";

describe("Exam Workspace", () => {
  it.todo("selects an answer option and marks active state");
  it.todo("jumps to target question from palette navigation");
  it.todo("updates timer progress based on elapsed time");
});
