import { describe, it } from "vitest";

describe("Dashboard", () => {
  it.todo("loads dashboard overview metrics in server component");
  it.todo("switches progress tabs via click and keyboard interaction");
});
