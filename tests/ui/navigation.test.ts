import { describe, it } from "vitest";

describe("Navigation", () => {
  it.todo("renders MainNav links and supports keyboard focus order");
  it.todo("renders Sidebar items with optional badges");
});
