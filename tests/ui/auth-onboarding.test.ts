import { describe, it } from "vitest";

describe("Auth Onboarding", () => {
  it.todo("updates selected onboarding goal using keyboard and pointer");
  it.todo("submits onboarding fields with valid date and study hours");
});
